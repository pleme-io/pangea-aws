# Code generated by pangea-forge.
# InSpec profile for verifying aws_autoscaling_lifecycle_hook infrastructure state.
#
# Usage:
#   1. Create the resource using Pangea or Terraform
#   2. Set AWS_TOKEN and AWS_GATEWAY_URL environment variables
#   3. Run: inspec exec profiles/aws_autoscaling_lifecycle_hook/ -t aws://

control 'aws_autoscaling_lifecycle_hook-exists' do
  impact 1.0
  title 'Manages a aws_autoscaling_lifecycle_hook resource'
  desc 'Verify that the aws_autoscaling_lifecycle_hook resource exists and has expected properties'

  describe aws_autoscaling_lifecycle_hook(name: attribute('id')) do
    it { should exist }
    its('autoscaling_group_name') { should_not be_nil }
    its('lifecycle_transition') { should_not be_nil }
    its('name') { should_not be_nil }
  end
end
