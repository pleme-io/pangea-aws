# Code generated by pangea-forge.
# InSpec profile for verifying aws_scheduler_schedule infrastructure state.
#
# Usage:
#   1. Create the resource using Pangea or Terraform
#   2. Set AWS_TOKEN and AWS_GATEWAY_URL environment variables
#   3. Run: inspec exec profiles/aws_scheduler_schedule/ -t aws://

control 'aws_scheduler_schedule-exists' do
  impact 1.0
  title 'Manages a aws_scheduler_schedule resource'
  desc 'Verify that the aws_scheduler_schedule resource exists and has expected properties'

  describe aws_scheduler_schedule(name: attribute('id')) do
    it { should exist }
    its('flexible_time_window') { should_not be_nil }
    its('schedule_expression') { should_not be_nil }
    its('target') { should_not be_nil }
  end
end
