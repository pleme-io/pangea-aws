# Code generated by pangea-forge.
# InSpec profile for verifying aws_invoicing_invoice_unit infrastructure state.
#
# Usage:
#   1. Create the resource using Pangea or Terraform
#   2. Set AWS_TOKEN and AWS_GATEWAY_URL environment variables
#   3. Run: inspec exec profiles/aws_invoicing_invoice_unit/ -t aws://

control 'aws_invoicing_invoice_unit-exists' do
  impact 1.0
  title 'Manages a aws_invoicing_invoice_unit resource'
  desc 'Verify that the aws_invoicing_invoice_unit resource exists and has expected properties'

  describe aws_invoicing_invoice_unit(name: attribute('id')) do
    it { should exist }
    its('invoice_receiver') { should_not be_nil }
    its('name') { should_not be_nil }
  end
end
