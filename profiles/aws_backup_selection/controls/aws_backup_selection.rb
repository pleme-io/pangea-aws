# Code generated by pangea-forge.
# InSpec profile for verifying aws_backup_selection infrastructure state.
#
# Usage:
#   1. Create the resource using Pangea or Terraform
#   2. Set AWS_TOKEN and AWS_GATEWAY_URL environment variables
#   3. Run: inspec exec profiles/aws_backup_selection/ -t aws://

control 'aws_backup_selection-exists' do
  impact 1.0
  title 'Manages a aws_backup_selection resource'
  desc 'Verify that the aws_backup_selection resource exists and has expected properties'

  describe aws_backup_selection(name: attribute('id')) do
    it { should exist }
    its('iam_role_arn') { should_not be_nil }
    its('name') { should_not be_nil }
    its('plan_id') { should_not be_nil }
  end
end
