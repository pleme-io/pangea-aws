# Code generated by pangea-forge.
# InSpec profile for verifying aws_codepipeline_webhook infrastructure state.
#
# Usage:
#   1. Create the resource using Pangea or Terraform
#   2. Set AWS_TOKEN and AWS_GATEWAY_URL environment variables
#   3. Run: inspec exec profiles/aws_codepipeline_webhook/ -t aws://

control 'aws_codepipeline_webhook-exists' do
  impact 1.0
  title 'Manages a aws_codepipeline_webhook resource'
  desc 'Verify that the aws_codepipeline_webhook resource exists and has expected properties'

  describe aws_codepipeline_webhook(name: attribute('id')) do
    it { should exist }
    its('authentication') { should_not be_nil }
    its('filter') { should_not be_nil }
    its('name') { should_not be_nil }
    its('target_action') { should_not be_nil }
    its('target_pipeline') { should_not be_nil }
  end
end
