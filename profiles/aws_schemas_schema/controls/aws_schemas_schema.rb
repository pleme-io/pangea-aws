# Code generated by pangea-forge.
# InSpec profile for verifying aws_schemas_schema infrastructure state.
#
# Usage:
#   1. Create the resource using Pangea or Terraform
#   2. Set AWS_TOKEN and AWS_GATEWAY_URL environment variables
#   3. Run: inspec exec profiles/aws_schemas_schema/ -t aws://

control 'aws_schemas_schema-exists' do
  impact 1.0
  title 'Manages a aws_schemas_schema resource'
  desc 'Verify that the aws_schemas_schema resource exists and has expected properties'

  describe aws_schemas_schema(name: attribute('id')) do
    it { should exist }
    its('content') { should_not be_nil }
    its('name') { should_not be_nil }
    its('registry_name') { should_not be_nil }
    its('type') { should_not be_nil }
  end
end
