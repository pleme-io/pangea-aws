# Code generated by pangea-forge.
# InSpec profile for verifying aws_dax_cluster infrastructure state.
#
# Usage:
#   1. Create the resource using Pangea or Terraform
#   2. Set AWS_TOKEN and AWS_GATEWAY_URL environment variables
#   3. Run: inspec exec profiles/aws_dax_cluster/ -t aws://

control 'aws_dax_cluster-exists' do
  impact 1.0
  title 'Manages a aws_dax_cluster resource'
  desc 'Verify that the aws_dax_cluster resource exists and has expected properties'

  describe aws_dax_cluster(name: attribute('id')) do
    it { should exist }
    its('cluster_name') { should_not be_nil }
    its('iam_role_arn') { should_not be_nil }
    its('node_type') { should_not be_nil }
    its('replication_factor') { should_not be_nil }
  end
end
