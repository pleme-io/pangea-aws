# Code generated by pangea-forge.
# InSpec profile for verifying aws_codepipeline infrastructure state.
#
# Usage:
#   1. Create the resource using Pangea or Terraform
#   2. Set AWS_TOKEN and AWS_GATEWAY_URL environment variables
#   3. Run: inspec exec profiles/aws_codepipeline/ -t aws://

control 'aws_codepipeline-exists' do
  impact 1.0
  title 'Manages a aws_codepipeline resource'
  desc 'Verify that the aws_codepipeline resource exists and has expected properties'

  describe aws_codepipeline(name: attribute('id')) do
    it { should exist }
    its('artifact_store') { should_not be_nil }
    its('name') { should_not be_nil }
    its('role_arn') { should_not be_nil }
    its('stage') { should_not be_nil }
  end
end
