# Code generated by pangea-forge.
# InSpec profile for verifying aws_subnet infrastructure state.
#
# Usage:
#   1. Create the resource using Pangea or Terraform
#   2. Set AWS_TOKEN and AWS_GATEWAY_URL environment variables
#   3. Run: inspec exec profiles/aws_subnet/ -t aws://

control 'aws_subnet-exists' do
  impact 1.0
  title 'Manages a aws_subnet resource'
  desc 'Verify that the aws_subnet resource exists and has expected properties'

  describe aws_subnet(name: attribute('id')) do
    it { should exist }
    its('vpc_id') { should_not be_nil }
  end
end
