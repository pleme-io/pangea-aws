# Code generated by pangea-forge.
# InSpec profile for verifying aws_lb_listener_rule infrastructure state.
#
# Usage:
#   1. Create the resource using Pangea or Terraform
#   2. Set AWS_TOKEN and AWS_GATEWAY_URL environment variables
#   3. Run: inspec exec profiles/aws_lb_listener_rule/ -t aws://

control 'aws_lb_listener_rule-exists' do
  impact 1.0
  title 'Manages a aws_lb_listener_rule resource'
  desc 'Verify that the aws_lb_listener_rule resource exists and has expected properties'

  describe aws_lb_listener_rule(name: attribute('id')) do
    it { should exist }
    its('action') { should_not be_nil }
    its('condition') { should_not be_nil }
    its('listener_arn') { should_not be_nil }
  end
end
