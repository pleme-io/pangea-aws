# Code generated by pangea-forge.
# InSpec profile for verifying aws_db_proxy infrastructure state.
#
# Usage:
#   1. Create the resource using Pangea or Terraform
#   2. Set AWS_TOKEN and AWS_GATEWAY_URL environment variables
#   3. Run: inspec exec profiles/aws_db_proxy/ -t aws://

control 'aws_db_proxy-exists' do
  impact 1.0
  title 'Manages a aws_db_proxy resource'
  desc 'Verify that the aws_db_proxy resource exists and has expected properties'

  describe aws_db_proxy(name: attribute('id')) do
    it { should exist }
    its('engine_family') { should_not be_nil }
    its('name') { should_not be_nil }
    its('role_arn') { should_not be_nil }
    its('vpc_subnet_ids') { should_not be_nil }
  end
end
