# Code generated by pangea-forge.
# InSpec profile for verifying aws_opensearch_package infrastructure state.
#
# Usage:
#   1. Create the resource using Pangea or Terraform
#   2. Set AWS_TOKEN and AWS_GATEWAY_URL environment variables
#   3. Run: inspec exec profiles/aws_opensearch_package/ -t aws://

control 'aws_opensearch_package-exists' do
  impact 1.0
  title 'Manages a aws_opensearch_package resource'
  desc 'Verify that the aws_opensearch_package resource exists and has expected properties'

  describe aws_opensearch_package(name: attribute('id')) do
    it { should exist }
    its('package_name') { should_not be_nil }
    its('package_source') { should_not be_nil }
    its('package_type') { should_not be_nil }
  end
end
